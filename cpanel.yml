---deployment:
  tasks:
    - export DEPLOYPATH=/home/leapux/public_html/dev1
    - /bin/cp .env.example .env
    - /usr/bin/npm install
    - /usr/bin/npm run build
    - /bin/cp -r build/* $DEPLOYPATH
